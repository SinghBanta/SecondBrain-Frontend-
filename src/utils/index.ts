// export function convertInstagramToEmbed(url: string) {
//     const match = url.match(/instagram\.com\/(p|reel|tv)\/([^/?]+)/);
//     if (match) {
//       const type = match[1];
//       const id = match[2];
//       console.log(`https://www.instagram.com/${type}/${id}/embed`)
//       return `https://www.instagram.com/${type}/${id}/embed`;
//     }
//     return null;
//   }

  